image: JAVA_HOME

stages:
  - build
  - test

build:
  stage: build
  script:
    - 'mvn package'
  only:
    - master
  artifacts:
    paths:
      - ./target/swagger-spring-*.jar

sonarqube:
  stage: test
  except:
    - master
  script:
    - 'mvn package sonar:sonar'

test:
  stage: test
  script:
    - 'mvn test'
  only:
    - merge_requests